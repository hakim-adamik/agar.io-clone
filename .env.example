DEBUG_SHOW_CELL_MASS=false


# Privy Configuration Example
# Copy this file to .env and fill in your values

# Get your Privy App ID from https://dashboard.privy.io
# 1. Sign up at https://dashboard.privy.io
# 2. Create a new app
# 3. Copy the App ID from the dashboard
PRIVY_APP_ID=your-privy_app-id

# =============================================================================
# NEON POSTGRES DATABASE CONFIGURATION
# =============================================================================
#
# Two separate databases for proper environment isolation:
# 1. PRODUCTION (agar-io-main) - Used by deploy.sh - Live user data
# 2. PREVIEW (agar-io-preview) - Used by deploy-preview.sh - Safe testing
#
# Get these from: https://vercel.com/dashboard/stores or https://console.neon.tech
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION (Simplified)
# =============================================================================
#
# Environment-specific database configuration:
# 1. LOCAL DEVELOPMENT: Uses preview database (safe, isolated from production)
# 2. PREVIEW DEPLOYMENT: Uses preview database (deploy-preview.sh sets this)
# 3. PRODUCTION DEPLOYMENT: Uses production database (deploy.sh sets this)
#
# The application uses process.env.DATABASE_URL exclusively
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONNECTION STRINGS
# -----------------------------------------------------------------------------
# Get these from: https://console.neon.tech or https://vercel.com/dashboard/stores

# Production database (agar-io-main) - Live user data
DATABASE_URL_PROD="postgresql://neondb_owner:your-prod-password@prod-host-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# Preview database (agar-io-preview) - Safe for testing and development
DATABASE_URL_PREVIEW="postgresql://neondb_owner:npg_X0hNZFwe8Lrk@ep-wild-bar-agks6pgk-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# -----------------------------------------------------------------------------
# ACTIVE DATABASE (what the application actually uses)
# -----------------------------------------------------------------------------
# For local development: Use preview database (recommended for safety)
DATABASE_URL="${DATABASE_URL_PREVIEW}"

# To test against production data locally (USE WITH CAUTION):
# DATABASE_URL="${DATABASE_URL_PROD}"

# Server Configuration
NODE_ENV="production"
PORT="8080"

# Configuration in Privy Dashboard:
# 1. Go to Login Methods and enable:
#    - Email
#    - Google
#    - Discord (optional)
#    - Twitter (optional)
# 2. Add allowed domains:
#    - http://localhost:3000 (for development)
#    - Your production domain