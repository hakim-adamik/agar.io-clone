# Privy Configuration Example
# Copy this file to .env and fill in your values

# Get your Privy App ID from https://dashboard.privy.io
# 1. Sign up at https://dashboard.privy.io
# 2. Create a new app
# 3. Copy the App ID from the dashboard
PRIVY_APP_ID=your-privy_app-id

# Neon Postgres Database Configuration
# Get these from your Vercel/Neon dashboard after creating a database
# 1. Go to https://vercel.com/dashboard/stores or https://console.neon.tech
# 2. Create a new Neon Serverless Postgres database
# 3. Copy all the connection strings from the dashboard

# Primary connection string (pooled) - Use this for most connections
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require"
POSTGRES_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/database?sslmode=require"

# Non-pooled connection for migrations/schema changes
DATABASE_URL_UNPOOLED="postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require"
POSTGRES_URL_NON_POOLING="postgresql://user:password@host.region.aws.neon.tech/database?sslmode=require"

# Additional connection variants
POSTGRES_URL_NO_SSL="postgresql://user:password@host-pooler.region.aws.neon.tech/database"
POSTGRES_PRISMA_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/database?connect_timeout=15&sslmode=require"

# Individual connection parameters
PGHOST="host-pooler.region.aws.neon.tech"
PGHOST_UNPOOLED="host.region.aws.neon.tech"
PGUSER="neondb_owner"
PGDATABASE="neondb"
PGPASSWORD="your-password"
POSTGRES_USER="neondb_owner"
POSTGRES_HOST="host-pooler.region.aws.neon.tech"
POSTGRES_PASSWORD="your-password"
POSTGRES_DATABASE="neondb"

# Server Configuration
NODE_ENV="production"
PORT="8080"
ADMIN_PASS="your-admin-password"

# Configuration in Privy Dashboard:
# 1. Go to Login Methods and enable:
#    - Email
#    - Google
#    - Discord (optional)
#    - Twitter (optional)
# 2. Add allowed domains:
#    - http://localhost:3000 (for development)
#    - Your production domain